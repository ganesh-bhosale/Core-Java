package oops.interfaces;
// Java program to illustrate the real world example of interface
interface Supercar {

    // all are the abstract methods

    void changeGear(int a);
    void speedUp(int a);
    void applyBrakes(int a);
}
